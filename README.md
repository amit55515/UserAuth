This application uses Node js and mongoDB atlas for user data persistence.
user athentication is done without using any external npm package.

to run the application run following commands in sequence
npm i
nodemon index